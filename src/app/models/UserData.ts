export class UserData {
  // tslint:disable-next-line:max-line-length
  constructor(public city: string, public currentPassword: string, public email: string, public firstname: string, public lastname: string, public numberHouse: string, public postCode: string, public street: string) {
  }
}

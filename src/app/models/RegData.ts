export class RegData {
  // tslint:disable-next-line:max-line-length
  constructor(public city: string, public email: string, public firstName: string, public houseNumber: string, public lastName: string, public nameAddress: string, public password: string, public postCode: string, public street: string, public username: string) {
  }
}
